@model IEnumerable<BooksShareMVC.Book>
@{
    ViewBag.Title = "Show";
}

<h2>Show</h2>

@if (Model != null)
{
    foreach (var item in Model)
    {
        @item.Title
        @item.Description
    }
}